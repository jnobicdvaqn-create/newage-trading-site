---
import BaseLayout from '@layouts/BaseLayout.astro';
import Breadcrumb from '@components/Breadcrumb.astro';
import { t } from '@i18n/index';
import type { Locale } from '@i18n/types';

export function getStaticPaths() {
  return [
    { params: { lang: 'en' } },
    { params: { lang: 'ru' } },
    { params: { lang: 'zh' } },
  ];
}

const { lang } = Astro.params as { lang: Locale };

const sections = [
  { title: t('terms.section1Title', lang), content: t('terms.section1Content', lang) },
  { title: t('terms.section2Title', lang), content: t('terms.section2Content', lang) },
  { title: t('terms.section3Title', lang), content: t('terms.section3Content', lang) },
  { title: t('terms.section4Title', lang), content: t('terms.section4Content', lang) },
  { title: t('terms.section5Title', lang), content: t('terms.section5Content', lang) },
  { title: t('terms.section6Title', lang), content: t('terms.section6Content', lang) },
];
---

<BaseLayout
  title={t('nav.terms', lang)}
  description={t('terms.description', lang)}
  lang={lang}
  breadcrumbs={[
    { name: t('nav.home', lang), path: `/${lang}/` },
    { name: t('nav.terms', lang), path: `/${lang}/terms/` },
  ]}
>
  <Breadcrumb crumbs={[
    { name: t('nav.home', lang), path: `/${lang}/` },
    { name: t('nav.terms', lang), path: `/${lang}/terms/` },
  ]} />

  <!-- Hero -->
  <section class="bg-primary py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h1 class="text-4xl font-bold text-white mb-4">{t('nav.terms', lang)}</h1>
      <p class="text-lg text-gray-300">{t('terms.lastUpdated', lang)}</p>
    </div>
  </section>

  <!-- Content -->
  <section class="container-section max-w-4xl mx-auto">
    <div class="prose max-w-none">
      {sections.map(section => (
        <div class="mb-10">
          <h2 class="text-2xl font-bold mb-4 text-primary">{section.title}</h2>
          <p class="text-muted leading-relaxed whitespace-pre-line">{section.content}</p>
        </div>
      ))}
    </div>

    <div class="mt-12 p-6 rounded-xl bg-surface border border-gray-200">
      <h3 class="text-lg font-semibold mb-3">{t('terms.contactTitle', lang)}</h3>
      <p class="text-muted">{t('terms.contactContent', lang)}</p>
      <p class="mt-2">
        <a href="mailto:felipeche01manager@YUNBSAOtrade.onmicrosoft.com" class="text-accent hover:underline">
          felipeche01manager@YUNBSAOtrade.onmicrosoft.com
        </a>
      </p>
    </div>
  </section>
</BaseLayout>
